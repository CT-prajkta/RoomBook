<h1 align="center"> Welcome to <%= @user.username %>'s page</p1>
<div class="row">
  <div class="col-md-4 col-md-offset-4 cenyer">
  <% gravatar_for @user, size:150 %>
  </div>
</div>
<h4 align="center"> <%= @user.username %>'s meet</p4>


<h4 align="center"><%= @user.username %>'s articles</h4>
<%= render 'meetings/meeting', obj: @user.meetings %>
<div>
    <%= link_to 'Edit', edit_user_path(@user)  %>
    
    
    <%= link_to "Delete user", user_path, method: :delete,
                    data: {  confirm: "Are you sure you want to delete the article?"},
                    class: "btn btn-xs btn-danger" %>
</div>